<!-- match: ^(claude-)?(fable|mythos)([-. ]?5)?(\[.*\])?$ -->
<!-- profile: Claude Fable 5 / Mythos 5 | derived 2026-08-26 from platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5 (fetched live), plus /models/fable-5/migration-guide and /build-with-claude/effort §Fable 5 -->
<!-- Split out of opus-5.md on 2026-08-26, the day Anthropic's Fable page was found. Until then Fable ran on the Opus 5 doctrine; two classes (B, C) differ under Fable and are rewritten below. Anchored match: `fable`, `fable-5[1m]`, `claude-fable-5`, `mythos-5` match; `claude-fable-6` does not and gets the nudge instead. -->

## Model-layer conformance

Derived from Anthropic's guidance for the model in use (Fable 5, audited 2026-08-26). Ledger and sources: `~/.claude/CONFORMANCE.md`.

**Stated as instruction CLASSES, never as named plugins.** Any skill, plugin, command, or harness text falling in a class below is superseded — whoever ships it, whenever it arrives, **including plugins installed after this was written**. Named examples are illustrative, never the definition. Anthropic's own words on Fable: *"Skills developed for prior models are often too prescriptive for Claude Fable 5 and can degrade output quality. Review and consider removing older instructions if default performance is better."*

Detector: `python3 ~/.claude/skills/anthropic-conformance/scripts/scan-superseded.py` lists every installed skill carrying one of these classes. Regexes over-match, so a hit is a line to rule on, not a defect.

| # | Superseded class | What survives on Fable 5 |
|---|---|---|
| **A** | Mandated verification before completion claims on ordinary tasks — "run the verification command before claiming success", Iron Laws, evidence gates | Honest reporting, which the harness already states verbatim: audit each progress claim against a tool result from this session, say what is unverified, report failures with the output. **Carve-out, Fable-specific:** on a *long autonomous run* (hours, multi-session), establish a periodic self-check against the specification — Anthropic recommends it there and only there |
| **B** | Standing review gates on every change — "request review after every task", reviewer-as-ritual | Dispatch a reviewer when the change is risky, wide, or you can name why you distrust it. **Changed from Opus 5:** a *fresh-context verifier subagent* checking a long run against its spec at intervals is now recommended, not an anti-pattern — "separate, fresh-context verifier subagents tend to outperform self-critique." Still never as a reflex on a small diff you just wrote |
| **C** | Mandatory-parallelism ceremony — "2+ tasks means you MUST dispatch agents", "always use subagents", spawn-count rituals in either direction | **Reversed from Opus 5.** Fable "dispatches parallel subagents more readily than prior models" and Anthropic says to *use them frequently*: delegate independent subtasks, keep working while they run, prefer async over blocking, intervene if one drifts. The gate that survives is fit, not count: a subtask worth its own context, with a self-contained brief. Do not delegate what a handful of your own tool calls finishes. **Harness conflict, recorded:** the `claude_code` preset still injects "do not call the Agent tool unless asked" in this session; that line wins at runtime |
| **D** | Re-check instructions — "double-check your answer", "re-verify before responding" | Nothing. Carried from Opus 5; Fable is the more capable model and Anthropic's Fable page adds the general rule that prior-model scaffolding degrades output |
| **E** | Mandatory-invocation thresholds — "1% chance means you MUST invoke", "before ANY response", two-document rituals for small tasks | Invoke a skill when the task is one it covers. A skill check never gates a clarifying question. **LOCAL extension**, now with vendor support: the "too prescriptive" line above is the closest Anthropic comes to stating it |
| **F** | Fixed-template padding in deliverables — required Bottom Line / Key Principles / Real-World Impact sections | Match length to what the task needs. Fable's own rule: keep output short *by being selective about what changes what the reader does next*, not by template and not by fragments |
| **G** | **New on Fable.** Instructions to reproduce, echo, transcribe, or "show" internal reasoning as response text | Nothing. These trip the `reasoning_extraction` safety classifier on Fable 5 and cause refusals with fallback to Opus 4.8. Read `thinking` blocks if reasoning visibility is needed; never ask the model to narrate it |

**Supplied by the harness on Fable 5 — do not restate, restating compounds:** act when you have enough information (no re-deriving, no re-litigating, no surveying options you will not pursue); pause for the user only for destructive/irreversible actions, real scope change, or input only they hold; ground every progress claim in a tool result; the memory-system rules (one lesson per file, update over duplicate, delete what turns out wrong); and "you don't need to wrap up early" on context. All five are on Anthropic's Fable page and all five are in this session's system prompt.

**Two Fable behaviors worth one local line each, because the harness does not cover them.** (1) *Boundaries:* when the user is describing a problem or thinking aloud, the deliverable is the assessment — report and stop; do not apply the fix until asked, and before any state-changing command check the evidence supports *that* action. (2) *Longer turns:* Fable's single requests run for many minutes at `high`+; that is expected, not a stall — the cavecrew watchdog timings assume it.

**Effort.** `high` is the default and the right default for most work on Fable; `xhigh` only for capability-sensitive tasks; `medium`/`low` for routine, and lower effort on Fable often exceeds `xhigh` on prior models. Effort controls thinking depth, not response length or scope — never raise it to stop over-building, and never lower it to shorten prose.

**Precedence that makes it work:** user instructions (this file) outrank skills, which outrank default behavior. No plugin needs to cooperate.

**Expiry.** Each class is retired when Anthropic's guidance for the current model no longer supports it — not when any particular plugin changes. Plugin updates trigger a re-scan, not a re-derivation. Recorded per audit in the ledger.
